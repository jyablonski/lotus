// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: user_game_bets.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const getUserGameBets = `-- name: GetUserGameBets :many
SELECT id, user_id, zone, amount, roll_result, payout, created_at FROM source.user_game_bets
WHERE user_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type GetUserGameBetsParams struct {
	UserID uuid.UUID
	Limit  int32
	Offset int32
}

func (q *Queries) GetUserGameBets(ctx context.Context, arg GetUserGameBetsParams) ([]SourceUserGameBet, error) {
	rows, err := q.db.QueryContext(ctx, getUserGameBets, arg.UserID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SourceUserGameBet
	for rows.Next() {
		var i SourceUserGameBet
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Zone,
			&i.Amount,
			&i.RollResult,
			&i.Payout,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const insertUserGameBet = `-- name: InsertUserGameBet :one
INSERT INTO source.user_game_bets (user_id, zone, amount, roll_result, payout)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, user_id, zone, amount, roll_result, payout, created_at
`

type InsertUserGameBetParams struct {
	UserID     uuid.UUID
	Zone       string
	Amount     int32
	RollResult int32
	Payout     int32
}

func (q *Queries) InsertUserGameBet(ctx context.Context, arg InsertUserGameBetParams) (SourceUserGameBet, error) {
	row := q.db.QueryRowContext(ctx, insertUserGameBet,
		arg.UserID,
		arg.Zone,
		arg.Amount,
		arg.RollResult,
		arg.Payout,
	)
	var i SourceUserGameBet
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Zone,
		&i.Amount,
		&i.RollResult,
		&i.Payout,
		&i.CreatedAt,
	)
	return i, err
}
