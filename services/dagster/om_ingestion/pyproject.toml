[project]
name = "om-ingestion"
version = "0.1.0"
requires-python = ">=3.10,<3.13"
dependencies = ["openmetadata-ingestion[postgres,dbt]==1.13.1.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true
