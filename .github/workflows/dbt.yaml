name: dbt CI / CD Pipeline

on:
  pull_request:
    paths:
      - "services/dbt/**"

env:
  UV_VERSION: "0.8.24"
  PYTHON_VERSION: "3.13"

jobs:
  dbt-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          sparse-checkout: |
            services/dbt/**
            docker/**
            Makefile
          sparse-checkout-cone-mode: false
