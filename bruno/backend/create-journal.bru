meta {
  name: Create Journal
  type: http
}

post {
  url: {{backend_url}}/v1/journals
  body: json
  auth: inherit
}

body:json {
{
  "userId": "string",
  "journalText": "string",
  "userMood": "string"
}
}
